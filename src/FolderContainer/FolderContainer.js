import React from 'react'

const FolderContainer = (props) => {
    return (
        <section>
            <FolderList/>
            <Folder/>
        </section>
    )
}

export default FolderContainer